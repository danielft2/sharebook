package com.example.sharebook.book_feature.data.remote.repository

import com.example.sharebook.book_feature.data.remote.service.RequestBookService
import javax.inject.Inject

class RequestBookRepository @Inject constructor(private val requestBookService: RequestBookService) {
}