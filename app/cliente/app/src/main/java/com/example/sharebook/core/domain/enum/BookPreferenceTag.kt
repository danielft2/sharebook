package com.example.sharebook.core.domain.enum

enum class BookPreferenceTag(val tag: String) {
    SEND("Prefência em enviar"),
    SEARCH("Prefência em buscar"),
    RECEIVE("Prefência em receber")
}